this is a pattern where numbers are printed  from top to down 
here i used for loop to run the code iteratively so  i can get i want to imagine
#!/bin/bash                     # it is standard header for bash in my terminal ,,, it depends on various systems 
read -p "enter the number of rows : " n
for row in `seq $n`
do
   for col in `seq $row`
   do
      echo "$row"
   done
   echo
done



pattern2
#!/bin/bash                     # it is standard header for bash in my terminal ,,, it depends on various systems 
read -p "enter the number of rows : " n
for row in `seq $n`
do
   for col in `seq $row`
   do
      echo "$n"
   done
   echo
done
